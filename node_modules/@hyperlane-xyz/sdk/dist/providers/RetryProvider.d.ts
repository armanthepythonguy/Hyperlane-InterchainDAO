import { ethers } from 'ethers';
export declare type RetryProviderOptions = {
    maxRequests: number;
    baseRetryMs: number;
};
export declare class RetryJsonRpcProvider extends ethers.providers
    .StaticJsonRpcProvider {
    readonly retryOptions: RetryProviderOptions;
    constructor(retryOptions: RetryProviderOptions, url?: ethers.utils.ConnectionInfo | string, network?: ethers.providers.Networkish);
    send(method: string, params: Array<any>): Promise<any>;
}
//# sourceMappingURL=RetryProvider.d.ts.map